# # Release-style builds for Flutter (Android APK + AAB, iOS without signing).
# # For App Store / Play Store uploads, add signing secrets and extend the iOS job.

# name: Build release

# on:
#   push:
#     branches: [main, master]
#   pull_request:
#     branches: [main, master]
#   workflow_dispatch:

# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true

# permissions:
#   contents: read

# jobs:
#   android:
#     name: Android release (APK + AAB)
#     runs-on: ubuntu-latest
#     timeout-minutes: 45

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Install Java 17
#         uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: "17"
#           cache: gradle

#       - name: Setup Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           channel: stable
#           cache: true

#       - name: Dependencies
#         run: flutter pub get

#       - name: Analyze
#         run: flutter analyze

#       - name: Test
#         run: flutter test

#       - name: Build APK (release)
#         run: flutter build apk --release

#       - name: Build App Bundle (release)
#         run: flutter build appbundle --release

#       - name: Upload Android artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: android-release
#           if-no-files-found: error
#           path: |
#             build/app/outputs/flutter-apk/app-release.apk
#             build/app/outputs/bundle/release/app-release.aab

#   ios:
#     name: iOS release (unsigned artifact)
#     runs-on: macos-latest
#     timeout-minutes: 50

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           channel: stable
#           cache: true

#       - name: Dependencies
#         run: flutter pub get

#       - name: Analyze
#         run: flutter analyze

#       - name: Test
#         run: flutter test

#       # Unsigned build — produces Runner.app for CI. Installable IPA needs codesign + provisioning profiles (secrets).
#       - name: Build iOS (release, no codesign)
#         run: flutter build ios --release --no-codesign

#       - name: Upload iOS artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: ios-release-unsigned
#           if-no-files-found: error
#           path: build/ios/iphoneos/Runner.app
